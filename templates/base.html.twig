<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <aside class="sidebar">
            <div class="sidebar-content">
                <div class="sidebar-logo">
                    <h2>📱 Social</h2>
                </div>
                <nav class="sidebar-nav">
                    <a href="{{ path('app_post_index') }}" class="nav-item">
                        <span class="nav-icon">🏠</span>
                        <span class="nav-text">Inicio</span>
                    </a>

                    {% if app.user %}
                        <a href="{{ path('app_post_new')}}" class="nav-item">
                            <span class="nav-icon">➕</span>
                            <span class="nav-text">Crear Publicación</span>
                        </a>
                        <a href="{{ path('app_user_index')}}" class="nav-item">
                            <span class="nav-icon">🔍</span>
                            <span class="nav-text">Buscar Usuarios</span>
                        </a>
                        <a href="{{ path('app_user_show', {'id': app.user.id}) }}" class="nav-item">
                            <span class="nav-icon">👤</span>
                            <span class="nav-text">Mi Perfil</span>
                        </a>
                        <a href="{{ path('app_logout') }}" class="nav-item nav-item-logout">
                            <span class="nav-icon">🚪</span>
                            <span class="nav-text">Cerrar Sesión</span>
                        </a>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="nav-item">
                            <span class="nav-icon">🔑</span>
                            <span class="nav-text">Iniciar Sesión</span>
                        </a>
                        <a href="{{ path('app_register') }}" class="nav-item">
                            <span class="nav-icon">📝</span>
                            <span class="nav-text">Registrarse</span>
                        </a>
                    {% endif %}
                </nav>
            </div>
        </aside>
        <main class="main-content">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
